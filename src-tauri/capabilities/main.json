{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "The initial workspace uses no privileged frontend commands.",
  "windows": ["main"],
  "permissions": []
}
